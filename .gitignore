build*/
cmake-build-*/
test/
.vscode/
.idea/
.cache/
*.swp
*.bat
*.bin
*.exe
*.gguf
output*.png
models*
*.log
preview.png

# longcat port: validation venvs + scratch (never commit)
.venv*/
.convert-tmp/
__pycache__/
*.pyc
# flux2 dev artifacts (not for the shared repo)
gallery/
profiles/
/tmp_*

# nsys/ncu binary traces (large; keep markdown writeups)
bench/results/*.nsys-rep
bench/results/*.sqlite
bench/results/*.ncu-rep

# vendored on demand by tools/m1_ref/cpp_port/build_basisu.sh (pinned BASISU_REF)
thirdparty/basis_universal/

# vendored on demand by tools/m1_ref/cpp_port/build_quadriflow.sh (pinned QF_REF)
thirdparty/QuadriFlow/

# vendored on demand, same as the two above. Both carry a nested .git, so without these lines a
# `git add -A` silently commits a BROKEN GITLINK STUB instead of the tree (163MB / 105MB).
thirdparty/ManifoldPlus/
thirdparty/instant-meshes/

# root-owned nsys/ncu profiling output from the docker bench runs (the rules above only cover
# bench/results/). Big, regenerable, and not ours to commit.
/m3b_*
